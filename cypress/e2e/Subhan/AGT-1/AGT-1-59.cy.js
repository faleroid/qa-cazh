describe('AGT-1.59	Checklist "Ubah Dokumen?" di form Edit	Field upload Dokumen muncul (required saat checkbox aktif)', () => {
  it('passes', () => {
    cy.visit('https://example.cypress.io')
  })
})